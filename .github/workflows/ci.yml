name: CI

on:
  pull_request:
  push:
    branches:
      - develop

permissions:
  contents: read

jobs:
  pipeline:
    needs: prepare
    uses: ./.github/workflows/pipeline.yml
    with:
      doRelease: false
