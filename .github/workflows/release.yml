name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  pipeline:
    needs: prepare
    uses: ./.github/workflows/pipeline.yml
    with:
      version: ${{ github.ref_name }}
      doRelease: true
